# 왜(Why)

한국 정치·정책의 인과관계를 지도로 보여주는 웹사이트.
"이 숫자가 왜 이렇게 됐나"를 결과에서 거꾸로 추적한다.
무료 공개. 광고·후원·구독 없음. **신뢰가 유일한 자산이다.**

## 구조

```
index.html          단일 HTML 파일. 데이터 + 렌더링 + 물리 레이아웃이 전부 여기.
                 아직 쪼개지 않는다. 렌더링 분리는 나중 단계다.
test/check.cjs   jsdom 점검 스크립트. 규칙 강제는 전부 여기에 있다.
docs/규칙.md      절대 규칙 7개 + 왜 그런 규칙이 있는지.
```

## 이 프로젝트에서 가장 중요한 원칙

**규칙은 프롬프트에 있으면 샌다. 반드시 자동 검사로 강제한다.**

이 파일에는 "왜 이 규칙이 있는지"만 쓴다. 강제는 전부 `test/` 밑에 둔다.
사람이 지키는 규칙은 언젠가 깨진다. `npm test`가 지키는 규칙만 남는다.

규칙 7개 전문과 그 이유는 [docs/규칙.md](docs/규칙.md)에 있다. 요약하면:

1. 확정된 공식 기록만 — 수사 중인 의혹은 절대 안 넣는다
2. 확인 안 된 숫자는 지어내지 말고 "확인 중"으로 비워둔다
3. **"재임 중" ≠ "밀어붙임"** — 대통령 관계는 공포일자 × 재임표 계산으로만 만든다
4. 시간 순서를 인과관계로 단정하지 않는다 — 결과 카드에 교란 요인을 함께 적는다
5. 법안마다 `limit`(남은 문제·비판) 칸을 채운다. 한쪽 진영에만 붙이지 않는다
6. 공공누리·퍼블릭도메인 사진만. `photoLicense` 없으면 실루엣으로 떨어진다
7. 모든 노드에 출처(`src`)를 표기한다

규칙 3이 가장 쉽게 깨진다. **발의자·소관부처 필드에서 대통령을 끌어오는 코드 경로를
아예 두지 않는다.** 그런 경로가 생기는 순간 "○○ 정부 때 통과된 법"이
"○○가 밀어붙인 법"으로 자동 변환된다.

## 배치는 데이터가 정한다 — 물리가 정하지 않는다

**반지름 = 연도**(뜻이 있다), **각도 = 흩뿌린 값**(뜻이 없다).
이 둘이 자리를 정한다. 물리는 **겹침을 푸는 데만** 쓴다.

전에는 링크 스프링에 맡겼다. 그랬더니:

| | 실측 |
|---|---|
| 목표 각도(`n.ang`) 분포 | 12방향에 **14~15개씩** — 완벽히 고르다 |
| 실제 위치 분포 | 12방향에 **2~19개** |
| 목표에서 벗어난 거리 | 중앙값 **111px**, 최대 307px |
| 반지름 어긋남 | 중앙값 **111px** — **연도가 거짓말이 된다** |

허브가 세게 당긴다 — 이재명 35선, 윤석열 30선.
그래서 첫 화면이 **부채꼴로 한쪽에 몰렸다.**

**연도 탓이 아니라 링크 탓이었다.** 반지름을 버릴 뻔했다.
반지름이 원인이면 고리 하나가 비어야지 부채꼴로 비지 않는다 — 그게 갈림길이었다.

고친 뒤: 각도 12~16개 · 반지름 어긋남 **0px** · 누른 뒤 멈춤 2.0초 → **0.75초**.

**선은 관련을 보여주는 것이지 자리를 정하는 것이 아니다.**

## 선은 인과가 아니다

**이 사이트는 인과를 주장하지 않는다.** 관련 있는 것들을 이어주고 판단은 보는 사람이 한다.

관계선의 뜻은 **"같은 주제·같은 시기에 있었다"** 이지 "이것 때문에 저렇게 됐다" 가 아니다.
그래서 선은 사실 판정이 가능하고, 나중에 자동 생성도 가능해진다.

**사람이 이해할 수 있는 근거가 안 나오는 연결은 애초에 만들지 않는다.**

카드의 관계 항목마다 `왜 이어졌나 · 같은 분야(의료·저출생) · 시기 1년 차이` 가 붙는다.
이 한 줄을 만들 수 없으면 그 연결은 넣지 않는다. 근거를 못 대는 선은 주장이지 기록이 아니다.

- `lead`(밀어붙임) · `against`(반대·대립) · `term`(그때 정권) 은 **공식 기록이라 그대로 둔다.**
- `l[3]==='result'` 는 **'결과' 가 아니라 '근거(l[5])가 붙은 연결'** 이라는 뜻이다.
  값 이름은 데이터 63건이 쓰고 있어 그대로 두고, 창고로 옮길 때 바꾼다.
- 방향(누가 앞이고 누가 뒤인지)으로 뜻을 만들지 않는다.
  전에는 같은 선이 보는 방향에 따라 '배경·원인' 과 '결과' 로 갈렸다 —
  그건 데이터가 아니라 화면이 만들어낸 주장이었다.

**지도에 그리는 선은 노드당 6개까지.** 실측으로 정한 값이다 —
이웃 6개까지 이름표가 100% 읽히고, 8개에서 63%, 10개를 넘으면 선 간격이 3.2° 로 붙는다.
**잘린 개수는 항상 화면에 밝힌다.** 말없이 자르면 "이게 전부" 라는 거짓말이 된다.

자세한 설계는 [docs/자동연결.md](docs/자동연결.md).

## 검사를 검사한다

**"통과했다"는 말을 믿기 전에, 그 검사가 무엇을 봤는지 확인한다.**

깨진 검사는 없는 검사보다 나쁘다. 없는 검사는 불안하게 만들지만,
깨진 검사는 안심시킨다. 안심한 채로 틀린 걸 배포한다.

실제로 이 프로젝트에서 **일곱 번** 나왔다. 일곱 번 다 초록불이었다.

| # | 무엇이 | 어떻게 거짓말했나 | 지금 막는 것 |
|---|---|---|---|
| 1 | `process.exit(0)` 하드코딩 | FAIL이 몇 건이든 `npm test`가 통과했다. 강제가 아니라 장식이었다 | `exit(fails.length?1:0)` |
| 2 | "겹침 0" | 노드 **원**의 중심거리만 쟀다. 실제로 겹치는 건 **이름표 박스**였다 (290쌍) | 이름표 박스 겹침 검사 |
| 3 | `measureText` 스텁 | `글자수 × 6.2px`. 한글은 12px 폰트에서 폭이 거의 12px다. **이름표 폭을 절반으로 재고 있었다** | 전각/반각 폭 계산 |
| 4 | 죽은 CSS 규칙 | 잘린 셀렉터가 다음 규칙을 삼켜 `body.panelon body.panelon .pclose`가 됐다. `body` 안에 `body`는 없으니 **영원히 매칭 안 된다.** 카드 ✕ 버튼이 브라우저 기본 회색 네모로 나왔다 | 12번 검사 |
| 5 | 스크립트 에러 무시 | `jsdomError`를 **모으고만 있었지 판정에 안 썼다.** 스크립트가 중간에 죽어도 PASS였다 | 14번 검사 |
| 7 | 셀렉터 중복 검사 | 블록 머리에 **앞선 주석이 그대로 붙어** 와서 같은 `.rolebar` 가 서로 다른 문자열이 됐다. 중복을 하나도 못 잡았다 | 머리에서 주석 제거 |
| 6 | `innerText` 로 화면 글자 검사 | **jsdom 은 `innerText` 를 지원하지 않는다.** `undefined` 가 나오고 `|| ''` 가 그걸 삼켜서, 화면에 실명을 되돌려 넣어도 검사가 통과했다 | `textContent` 사용 |

**공통점: 일곱 번 다 문법 오류가 아니었다.** 코드는 멀쩡해 보였고 검사는 초록불이었고
화면만 틀렸다. 그래서 사람이 눈으로 보기 전까지 아무도 몰랐다.

**6번과 7번은 검사를 새로 만들면서 그 자리에서 났다.** 둘 다 실패를 주입해 보지 않았으면
거짓 보증을 그대로 커밋할 뻔했다. 7번은 "중복을 하나도 못 잡는 중복 검사" 였다 —
**아무것도 안 잡는 검사는 언제나 PASS 라서 가장 오래 산다.**
**새 검사를 넣을 때마다 실패를 주입해서 실제로 FAIL 이 나는지 본다** — 아래 1번이 그 이유다.
그리고 **jsdom 은 브라우저가 아니다.** `innerText` `offsetWidth` `getComputedStyle` 의
레이아웃 값처럼 지원이 없거나 다른 API 가 있다. 스텁·폴백이 값을 삼키면 검사는 조용히 통과한다.

**5번이 제일 무섭다.** 스크립트가 중간에 죽어도 **함수 선언은 호이스팅되어 이미 정의돼 있다.**
그래서 카드도 열리고 검색창도 보인다. 겉으로 멀쩡한데 그 뒤 초기화가 전부 안 돈다.
3번도 같은 종류다 — 2번을 고쳐도 겹침이 절반만 잡혔을 것이다.

**검사를 고쳤다는 사실이 그 검사가 맞다는 뜻은 아니다.**

## CSS 규칙 — 미디어쿼리는 항상 파일 맨 끝

**미디어쿼리는 `<style>` 맨 끝에 모은다. 그 뒤에 일반 규칙을 쓰지 않는다.**

같은 특정도면 **나중에 선언한 쪽이 이긴다.** 미디어쿼리 뒤에 일반 규칙을 두면
반응형이 조용히 무력화된다. 문법 오류가 없어서 아무도 모른다.
화면만 틀리고, 코드는 멀쩡해 보인다.

세 번 났다. **세 번 다 같은 원인인데 각각 다른 버그처럼 보였다.**

| 셀렉터 | 겉으로 보인 증상 |
|---|---|
| `.brand` | 폰에서 제목이 "왜왜"로 두 번 렌더 |
| `.search input` | 폰 검색 안내문이 사라짐 (입력창은 388px로 넓게 열린 채) |
| `.ub` | '처음으로' 버튼이 필터를 안 눌러도 항상 보임 |

앞의 둘은 `font-size:0` / `width:38px`으로 **숨기려던 것이 안 숨겨진** 경우다.
세 번째는 `[hidden]`을 이겨버린 경우다. 개별 버그로 고치면 네 번째가 또 온다.

**강제:** `test/check.cjs` 11번 검사. 미디어쿼리 뒤에 일반 규칙이 하나라도 있으면 **FAIL**.
미디어쿼리 안에 같은 셀렉터가 있으면 "무엇을 덮어쓰는지"까지 지목한다.

### 그래서 생긴 두 번째 규칙

미디어쿼리를 뒤로 몰면서 **"같은 특정도면 뒤엣것이 이긴다"가 이 파일의 기본 구조**가 됐다.
그 구조를 자꾸 잊는다.

**`display`를 끄려면 앞에 `none`을 넣지 말고, 뒤에 있는 원본 규칙을 고쳐라.**

```css
.rolebar{display:none}                    /* ✗ 앞에 넣어봐야 */
.rolebar{position:fixed;display:flex;…}   /*   뒤엣것이 이긴다 */
```

`.rolebar` 를 이렇게 껐다가 화면에 그대로 보였다. 브라우저에 규칙을 물어보고서야 알았다.
`display` 뿐 아니라 모든 속성이 같다 — **끄고 싶으면 켜는 곳을 고친다.**

**강제:** 12번 검사.
- 앞의 `display:none` 이 뒤의 `display:…` 에 지는 경우 → **FAIL**
- 그 밖에 같은 셀렉터를 뒤에서 덮어써 앞 선언이 죽은 경우 → WARN (현재 14건, 정리 대상)

### 그리고 세 번째 규칙 — 문서에 쓴 조건이 코드에도 반영됐는지 확인한다

**말과 값이 따로 놀면 문법 오류가 없다.** 그래서 아무도 모른다.

두 번 났는데 둘 다 같은 종류다.

| 문서에 쓴 말 | 코드가 한 일 |
|---|---|
| "표결은 20대부터만 제공된다" | 전 대수로 시간을 셌다 (27.8시간 vs 실제 17.8시간) |
| 표 머리 "1초 간격" | 실행 시 간격(GAP_MS)으로 계산 — 0으로 테스트하니 0.0시간 |

**같은 쪽에 조건을 써놓고 숫자는 그 조건을 안 지켰다.**
글을 읽는 사람은 숫자를 믿고, 숫자를 만든 코드는 글을 안 읽는다.

- 문서에 조건을 쓰면 **그 조건이 코드 어디에 있는지** 같이 적는다
- 조건 값은 **한 곳에만 둔다.** `VOTE_MIN_AGE` 는 `tools/lib/api.mjs` 하나뿐이고
  `count` 와 `collect` 가 그걸 import 한다. 각자 갖고 있으면 어긋난다
- 표 머리에 단위·조건을 썼으면 **그 단위로 계산한다**

### 값을 잴 때는 전환(transition)을 끄고 잰다

**브라우저 값도 그대로 믿으면 안 된다.** 이번이 세 번째다.

`.rail` 의 `transform` 이 `translateY(1492px)` 로 나와서 "901~1000 구간에서 패널이
화면 밖" 이라는 버그를 보고할 뻔했다. **멈춰 있던 전환의 잔상이었다.**

```js
var st=document.createElement('style');
st.textContent='*{transition:none !important;animation:none !important}';
document.head.appendChild(st);
el.getBoundingClientRect();          // 강제 리플로
var v=getComputedStyle(el).transform;  // 이제 진짜 값
st.remove();
```

- **`getComputedStyle` · `getBoundingClientRect` 를 읽기 전에 전환을 끈다**
- 브라우저 창이 뒤로 가면 전환과 `requestAnimationFrame` 이 멈춘다.
  그 상태에서 잰 값은 **중간값**이지 최종값이 아니다
- 재기 전에 탭을 앞으로 가져오거나, 위처럼 전환을 끄고 리플로를 강제한다

**`getBoundingClientRect` 는 transform 을 포함한다 — 전환에 휘둘린다.**
배치값이 필요하면 **`offsetLeft` · `offsetWidth` · `offsetTop`** 을 쓴다. 이건 transform 을 안 탄다.

왼쪽 패널이 지도를 얼마나 가리는지 `getBoundingClientRect().right` 로 쟀더니
전환 잔상 때문에 **230px 이 0 으로** 나왔다. 그래서 지도가 패널 뒤로 밀렸다.
`offsetLeft + offsetWidth` 로 바꾸니 언제 재도 230px 이다.

| 무엇을 재나 | 쓸 것 |
|---|---|
| 화면에서 실제로 차지하는 자리 (transform 포함) | `getBoundingClientRect` — **전환을 끄고** |
| 배치상의 위치·크기 (transform 무시) | `offsetLeft` `offsetWidth` `offsetTop` |
| 내용이 넘치는가 | `scrollHeight` vs `clientHeight` |

**jsdom 은 값을 안 주고, 브라우저는 틀린 값을 준다.** 종류가 다른 거짓말이다.

### 검사는 계산으로 값을 만들지 말고 페이지에서 직접 읽는다

**계산으로 만들면 화면과 갈라진다. 그리고 그때 우리는 검사 쪽을 안 의심한다.**

`화면상 글자 = 글꼴 × 배율` 로 계산했는데 `labelFontScale()` 항이 빠져 있었다.
1440·1920 에서 **"5.5px, 읽을 수 있는 하한 11px 미만"** 경고가 며칠 떴다.
실제로는 12.5px 이었다. **없는 문제를 걱정하고 있었다.**

```js
/* ✗ 검사가 공식을 베껴 쓴다 — 코드가 바뀌면 조용히 갈라진다 */
const labPx = LABEL_FONT_PX * camS;

/* ✓ 페이지에서 실제 값을 읽는다 */
const labPx = win.eval('(function(){return 12.5*labelFontScale()*cam.s})()');
```

이름표 겹침도 같은 이유로 `labelBox(n)` 을 **페이지에 두고 검사가 그걸 부른다.**
검사가 상자를 다시 계산하면 화면이 그리는 상자와 달라진다.

- **화면이 쓰는 함수를 export 하고 검사가 그것을 부른다**
- 공식을 검사에 베껴 쓰지 않는다. 두 벌이 되는 순간 갈라진다
- 거짓 경보는 거짓 통과만큼 나쁘다 — **없는 문제를 고치느라 있는 문제를 못 본다**

### 상수는 쓰는 곳보다 앞에 선언한다

`var` 는 **선언만 끌어올리고 값은 안 끌어올린다.**

```js
var NARROW_MIN_S = LABEL_FAR;   /* ✗ undefined */
…
var LABEL_FAR = 0.62;
```

이렇게 두었더니 이름표가 **171/171 개** 그려지고 겹침이 **314쌍** 났다.
문법 오류가 아니고 에러도 안 난다. **화면을 보기 전까지 모른다.**

**강제:** 24번 검사. 최상위 `var` 의 초기값이 **더 뒤에서 선언된 최상위 `var`** 를
쓰면 FAIL. 이름과 줄 번호를 함께 지목한다.

### 약속을 바꾸면 그 약속을 재던 검사도 같이 바꾼다

**안 바꾸면 옛 규칙이 검사 안에서 살아남는다.**

첫 화면의 약속을 "가장 큰 결과 노드 하나가 중앙" 에서
"보이는 노드 무리의 중심이 중앙" 으로 바꿨다.
화면은 좋아졌는데 **검사 21번이 FAIL 을 냈다** — 옛 약속을 재고 있었기 때문이다.

이때 하기 쉬운 잘못이 두 가지다.
- 검사가 FAIL 하니 **화면을 옛날로 되돌린다** (검사가 설계를 지배한다)
- 검사의 한계값만 느슨하게 풀어 통과시킨다 (검사가 아무것도 안 잡게 된다)

**옳은 처리는 검사를 새 약속으로 다시 쓰는 것이다.**
21번에서 '노드 하나가 중앙' 판정을 걷어내고 26번(무리 중심)으로 옮겼다.
21번에는 그 검사만 할 수 있는 것 — '화면이 커진 뒤' — 을 남겼다.

- 설계를 바꾸면 **어떤 검사가 그걸 재고 있었는지 먼저 찾는다**
- 검사가 FAIL 하면 **화면이 틀린 건지 검사가 낡은 건지 먼저 가른다**
- 낡은 검사는 지우지 말고 **그 검사만 할 수 있는 것을 남긴다.**
  통째로 지우면 그 자리에 있던 다른 보증까지 사라진다

### 검사는 사람이 하는 짓을 흉내 내야 한다

**첫 화면이 여섯 번 어긋났다. 검사는 여섯 번 다 통과했다.**

마지막 원인은 `camUser` 가 **1px 이동에도 켜지는 것**이었다.
탭 한 번도 미세한 이동을 만들기 때문에, 로드 중에 화면을 **한 번만 만지면**
첫 화면 맞춤이 영원히 취소되고 카메라가 init 값(배율 1.0, 원점 좌상단)에 머문다.
"왼쪽에 확대된 채로 뜬다" 가 이것이었다. 폰에서는 거의 항상 걸린다.

**검사가 손가락을 흉내 내지 않아서 못 잡았다.**
12개 해상도에서 물리를 돌리고 카메라를 쟀지만, 그 사이에 아무도 화면을 만지지 않았다.
실제 사람은 로드가 끝나기 전에 만진다.

흉내 낼 것 — 전부 실제로 사람이 하는 짓이다:
- **로드 중에 화면을 만진다** (검사 28번)
- 스크롤한다 · 주소창이 접혀 화면이 커진다 (검사 21번)
- **빠르게 여러 번 누른다** — 앞 동작이 안 끝났는데 다음을 누른다
- 화면을 돌린다 · 폴드를 접었다 편다
- 뒤로 갔다 다시 온다

**"가만히 두면 잘 된다" 는 검사는 아무것도 보증하지 않는다.**
사람은 가만히 있지 않는다.

### 사람이 무엇을 보는지를 재라

**모든 것을 똑같이 세면 눈과 갈라진다.**
큰 것, 밝은 것, 색이 있는 것을 사람은 먼저 본다.

첫 화면 쏠림을 **여덟 번** 헤맸다. 마지막까지 남은 이유가 이것이다.

| | 값 |
|---|---|
| 전체 노드의 무리중심 | **15px** — 좋다 |
| 결과 노드(큰 노란 원) 사분면 | **4/0/1/0** — 오른쪽이 통째로 빔 |
| 결과중심 어긋남 | **202px** |

작은 점 171개가 고루 퍼져 있어 평균은 좋게 나왔다.
그런데 **화면 인상을 만드는 것은 큰 노란 원 19개**다.
검사는 "고르다" 고 했고 사람은 "왼쪽으로 쏠렸다" 고 했다. 둘 다 맞았다.

- 무엇이 눈에 띄는지 먼저 정하고, **그것을 따로 센다**
- 평균 하나로 뭉개지 않는다. 종류별로 나눠 본다
- 검사 숫자와 눈이 갈라지면 **검사가 다른 것을 재고 있는 것**이다

### 검사가 실행마다 흔들리면 한도를 푸는 게 아니라 재는 방법을 고친다

882×344 에서 무리중심이 **9.0% / 9.1%** 를 오가며 한도 9% 를 넘었다 안 넘었다 했다.
**이런 검사는 있으나 마나다. 언젠가 우연히 통과한다.**

하기 쉬운 잘못은 한도를 10% 로 올리는 것이다. 그러면 흔들림은 멈추지만
검사는 아무것도 안 잡게 된다.

**옳은 처리는 왜 흔들리는지 찾는 것이다.**
그 해상도에서는 결과 노드가 3개만 보이는데 그 셋으로 중심을 잡고 있었다.
셋으로는 분포가 안 된다. 다섯 개 이상일 때만 쓰게 고치니 **1.2~1.3%** 로 안정됐다.

- 한도에 딱 붙은 값은 **재는 방법이 잘못됐다는 신호**다
- 표본이 너무 적어서 흔들리는 경우가 가장 흔하다
- 고친 뒤 **연속으로 여러 번 돌려** 같은 값이 나오는지 본다

### 검사도 버그와 똑같은 맹점을 가질 수 있다

캔버스는 **같은 값을 대입해도** 비트맵이 지워진다 (`255,0,0,255` → `0,0,0,0`).
그래서 `size()` 가 돌 때마다 화면이 한 프레임 비었다 —
"사건을 누르면 화면이 꺼졌다 켜진다" 가 이것이었다.

이걸 잡으려고 만든 검사 35번이 처음에 **값이 바뀔 때만** 지움으로 셌다.
옛 코드를 주입했는데 **통과했다.** 검사가 버그와 같은 가정을 하고 있었다.

**실패 주입이 통과하면 검사를 의심한다.** 코드가 고쳐진 게 아니다.

### 빈 것 자체는 잘못이 아니다. 말없이 비우는 것이 잘못이다

결과 노드 19개에 3관문용 핵심어를 넣었는데, 둘은 넣을 말이 없었다.
방첩사 개편(r3)은 `기무사` `안보지원` `방첩` `보안사` `정보사` 가
공포 법안 18,158건에서 **전부 0건**이다. GOP 경계병력(q5)도 `경계` `병력` 이 0건이다.

**진짜 이유는 그 일이 법률이 아니라 대통령령·기본계획으로 이뤄졌기 때문이다.**
법안명에 흔적이 없는 것이 당연하다.

그때 넓은 말로 억지로 붙이면 무관한 법이 온다 —
실제로 r3 에는 「국군포로의 송환 및 대우 등에 관한 법률」이, q5 에는 군인연금법이 붙었다.
**그건 우리가 만든 거짓 연결이다.**

**"이 결과는 자동으로 이을 근거를 못 찾았습니다" 는 정직한 답이다.**
비우고 화면에 밝힌다. 말없이 비우면 "이을 게 없었다" 와 "우리가 안 이었다" 가
구별되지 않는다. **강제:** 창고 검사 F.

### 이름 안에 다른 것이 섞여 있는지 본다

핵심어 `장병` 이 13건 걸렸는데 **전부 '장병완 의원'** 이었다.
`BILL_NM` 이 `병역법 일부개정법률안(장병완의원 등 32인)` 꼴로 발의자를 괄호에 담기 때문이다.
괄호를 떼니 0건이 됐다. 그대로 갔으면 **사람 이름으로 법을 이었다.**

| 필드 | 어느 표 | 발의자 괄호 |
|---|---|---|
| `BILL_NAME` | 발의법률안 · 개인별 표결 | **없다** (0/400) |
| `BILL_NM` | 본회의 처리 | **항상 있다** (400/400) |

- 문자열을 대조하기 전에 **그 문자열이 무엇으로 이뤄져 있는지** 먼저 본다
- 같은 뜻인데 표마다 이름도 형식도 다르다. 어느 표에서 왔는지가 곧 전처리를 정한다

### 그래서 지킬 것

1. **검사에 실패를 주입해서 실제로 FAIL이 나는지 본다.**
   노드 하나의 `src`를 일부러 지운 사본으로 돌려 `exit 1`을 확인하는 식이다.
   통과하는 것만 보고 검사를 믿지 않는다.
2. **검사 이름에 무엇을 재는지 박는다.** "겹침"이 아니라 "점겹침"·"이름표겹침"이다.
   이름이 모호하면 사람이 알아서 넓게 해석한다.
3. **0은 의심한다.** 0건은 "문제 없음"과 "안 보고 있음"을 구별하지 않는다.
   `이름표 0/171개 그려짐`처럼 **분모를 같이 출력한다.**
4. **가짜 값(스텁·목)은 실물과 대조한다.** jsdom 숫자와 브라우저 숫자를 나란히 본다.
   지금은 이름표 겹침이 jsdom 291쌍 : 브라우저 268쌍으로 맞는다. 안 맞으면 스텁이 틀린 것이다.
5. **계산이 통과해도 눈으로 본다.**
   부채꼴 모으기를 만들 때 수치는 전부 정상(6/6 · 반지름 흔들림 0)이었는데
   **화면에서 원 세 개가 포개져 있었다.** 각도를 충돌 해소 뒤에 강제해
   충돌 분리를 덮어쓴 탓이다. 스크린샷을 안 봤으면 그대로 커밋했다. 계산으로 못 잡는 게 있다.
   결과 노드 전체의 중심으로 화면을 맞췄더니 고리 배치라 한가운데가 빈 곳이었다.
   닫기 버튼이 서랍 내용을 가렸다. 둘 다 수치는 정상이었고 화면만 틀렸다.

## 데이터 구조

노드 · 관계 · 역할 세 가지뿐. 나라가 바뀌어도 틀은 동일하다.

- **노드** `N` — `{id, t, side, lab, src, ...}`
  - `t`: `bill`(법안) / `result`(결과) / `person` 등
  - `side`: 진영. `blue` / `red` / `gov` / `gold`
  - `ghost`: 런타임 파생 노드. 선언 164개 + 파생 67개 = 실제 231개
- **관계** `L` — `[from, to, label, role, sentence, evidence]`
  - `role`: `lead`(밀어붙임) / `term`(그때 정권, 보라색) / 등
  - `l[5]`(evidence)가 있어야 인과 관계로 인정된다

## 작업 방식

```bash
npm test
```

- 파일을 고칠 때마다 `npm test`를 돌리고, **통과한 것만 커밋한다.**
- **통과했다는 출력을 그대로 믿지 않는다.** 위 "검사를 검사한다"를 볼 것.
- **정규식으로 코드 블록을 삭제하지 마라.** 문법 검사는 통과하는데 함수가 통째로
  날아갈 수 있다. 실제로 그런 사고가 한 번 났다. 반드시 jsdom으로 띄워서 확인한다.
- 해상도 **412(폰) · 840(폴드) · 1440 · 1920** 에서 모두 확인한다. `check.cjs`가 자동으로 돈다.
- 확인 안 된 사실은 넣지 말고, **뭘 확인 못 했는지 말한다.**

## 지금 알려진 상태

- **진영 분포가 기울어 있다** (blue 93 / red 55 / gov 35). 규칙 5와 부딪히는 지점.
  API로 발의·표결 데이터를 채우면 자동으로 완화된다. **손으로 고치지 않는다.**
- `limit` 칸이 빈 법안 4개: `b6` `b7` `b9` `b11`. 지금은 WARN.
- **결과 노드가 19개뿐이다. 이게 진짜 병목이다.**
  법안 수가 아니라 결과 숫자가 성장 단위다.
- `official2` 링크가 붙은 노드는 `r6` 하나뿐.
- API 수집기는 아직 없다. 인증키가 없어서다.

## 창고 (SQLite)

수집물은 `db/warehouse.db` 에 담고 **커밋하지 않는다.** 스키마와 규칙만 남긴다.

```
db/schema.sql       원본층 · 가공층 · 관계층
db/rules/*.sql      선을 만드는 규칙. 규칙 하나가 파일 하나다
db/seed.sql         규칙 목록 (rule.reads = 그 규칙이 읽어도 되는 표)
tools/count.mjs     대수별 건수 세기. 수집 전에 규모를 먼저 안다
tools/collect.mjs   원본층 수집기. 중단해도 이어서 받는다
test/warehouse.cjs  검사 A~E
```

**선은 규칙 없이 못 들어간다.** `link.rule` 이 `NOT NULL REFERENCES rule(name)` 이고
`link.why`(왜 이어졌나)도 `NOT NULL` 이다. 근거를 못 대는 선은 주장이지 기록이 아니다.

**규칙 3 은 `rule.reads` 로 강제한다.** `term_by_promulgation` 이 읽어도 되는 표는
`bill, president_term` 둘뿐이다. 검사 B 가 `.sql` 원문을 훑어 그 밖의 표가 나오면 FAIL.
검사 D 는 **발의자에서 대통령을 끌어오는 가짜 규칙을 주입해 B 가 실제로 FAIL 하는지 본다.**
D 를 빼지 않는다 — 아무것도 안 잡는 검사는 언제나 PASS 라서 가장 오래 산다.

**API 가 안 주는 시기가 두 군데 있다.** 실측이다 (`probe-out/_COUNT.md`).

| | 제공 | 못 얻는 시기 |
|---|---|---|
| 발의법률안 | 제10대~ | **제1~9대 (1948~1979) 전부 0건** |
| 개인별 표결 | 제20대~ | 제19대 이전 |

**0 은 "법안이 없었다" 가 아니라 "API 가 안 준다" 는 뜻이다.**
지도의 **1945~1980 근현대사 분야는 이 구멍 위에 있다** — 자동 수집이 채워주지 않는다.
법제처 근대법령을 확인해야 하는데 아직 못 했다.
수집기는 건너뛴 대수를 화면에 밝힌다. 말없이 건너뛰면
"표결이 없는 법안" 과 "표결을 안 받은 법안" 이 구별되지 않는다.

자세한 건 [docs/창고설계.md](docs/창고설계.md).

## 화면에 안 그리는 것으로는 못 막는다

이 사이트는 정적이다. **JSON 을 통째로 받아서 그린다.**
법안 카드에 찬성자 이름을 안 늘어놓아도, 내보낸 파일에 들어 있으면
주소창에 파일 이름만 치면 전부 나온다.

**그래서 이건 화면 결정이 아니라 내보내기 결정이다.**
개인 표결은 인물별 파일로 쪼갠다 — 전량을 받으려면 3,295번 받아야 한다.
못 막지만 늦춘다. **우리가 명단을 만들어 배포하는 쪽이 되지 않는 것**이 목적이다.

받되 내보내지 않는 필드가 있다: `BIRDY_DT` `NAAS_TEL_NO` `NAAS_EMAIL_ADDR`.
창고에는 원본이 남고 지도로는 안 나간다. 매퍼가 `member` 표에 넣지 않는다.

카드 설계와 위험 목록은 [docs/인물법안카드.md](docs/인물법안카드.md).

## 로드맵

한국판 완성 → 미국 · 일본 확장 → 다국어 지원.

### 글자가 같은 것과 주제가 같은 것은 다르다

**「외국인투자세액감면」과 「외국인 유권자」는 글자만 같고 주제가 다르다.**

「중국 국적 81%」(외국인 유권자 중 중국 국적 비율)에 74개가 붙어 있었다.
그중 **69개가 한 관문에서 왔고 전부 무관했다** — 사건명에 결과의 핵심어가 있고
시기가 ±3년이면 잇는 관문이었다.

    외국인투자세액감면 대상이 아님
    베트남에 둔 고정사업장 외국납부세액공제
    성매매알선등행위처벌법위반 · 출입국관리법위반

`외국인` 이라는 **글자**가 있으면 붙었다. 세금 판례와 형사 판례가 유권자 비율에 붙었다.

같은 일이 분야 사전(`db/cat_keys.json`)에서도 났다. 결과의 좁은 핵심어에 분야 사전을
**더해서** 쓰고 있었는데 그 사전의 낱말이 너무 넓다 — `세법` 2,445건 · `안전` 2,205건 ·
`외국` 300건대(공포 법안 기준). `외국` 하나로 「외국공무원에 대한 뇌물방지법」이 딸려 왔다.

- **넓히는 것은 관문이 할 일이 아니다.** 결과가 스스로 적은 좁은 핵심어만 쓴다.
  더 잇고 싶으면 그 결과의 `keys` 를 늘린다 — 사람이 확인하고 넣는 값이라 근거가 있다
- 핵심어를 넣기 전에 **그 말이 공포 법안 몇 건에 걸리는지 센다.** 수백 건이면 그건 핵심어가 아니다
- 붙은 것을 관문별로 갈라 세어 본다. 한 관문이 대부분을 끌어오면 그 관문이 범인이다

### 판례는 통계가 아니라 법에 붙는다

**판결은 그 법을 어떻게 적용할지 정한 것이지 통계 숫자를 만든 것이 아니다.**

판례·헌재결정을 결과에 직접 이으면 두 가지가 한꺼번에 깨진다.
**뜻이 안 맞고**(위 참조), **양이 압도한다** — 「집값 5.36% 상승」에 이어진 1,528개 중
1,347개가 판례였다. 관련된 것을 보여주는 게 아니라 검색 결과를 쏟아붓는 것이 된다.

그래서 길을 하나로 한다.

    결과 ──(분야·시기·핵심어)──▶ 법 ──(참조조문·심판대상조문)──▶ 판례·헌재

법에 붙이는 관문은 **재판부가 스스로 "이 조문을 심판했다" 고 적은 것**이라 근거가 세다.
결과 카드에는 법 3~5개가 남고, 그 법을 눌러야 판례가 보인다.
실측: 결과당 이웃 중앙값 85 → **6개**(최소 1 · 최대 14).

그리고 **판례는 지도에 안 그린다.** 「외국인인 상속인 특정이 불가능한 경우 채권자 대위 가부」·
「(심리불속행기각)쟁점 이자수익은 조세특례제한법 제121조의2…」 는 법률가용 사건명이다.
읽을 수 없는 노드는 지도에서 자리만 차지한다. **지우는 게 아니라 옮기는 것이다** —
검색·카드 목록·'모두 보기' 에는 그대로 있다.

### 안 그리는 것은 밀지도 마라 — 겹침 상자는 화면과 같아야 한다

**물리는 화면에 그리는 상자를 밀어야 한다.** 다른 상자를 밀면 두 벌이 되고 갈라진다.

평소 화면을 '결과만 또렷 · 나머지는 흐린 점' 으로 바꿨는데, 물리는 그대로
**이름표 크기(가로 100px 남짓)로 겹침을 풀고 있었다.** 화면에는 4px 짜리 점인데.

| 노드 | 한 틱 (고치기 전) | 뒤 |
|---:|---:|---:|
| 5,000 | 3,097ms | **8.1ms** |
| 20,000 | 11,020ms | 50ms |

고친 것은 세 줄이다.

- `hw`/`hh` 가 점으로 그리는 노드에는 점 크기를 돌려준다
- **겹침을 푸는 대상**을 이름표를 그리는 노드로 좁힌다 (4,940 → 19)
- **밀어내기**도 같은 대상으로 좁힌다

**계산을 빠르게 한 게 아니라 안 해도 되는 계산을 뺐다.**
전에 격자로 바꿔 O(N²)를 없앴을 때도 이 일은 남아 있었다 —
격자는 *안 해도 되는 일을 빨리* 하게 했을 뿐이다.

**그리고 검사도 같이 틀리고 있었다.** 검사 22(고리 밀도)가 `lw`·`lh` 로
**자기 공식을 베껴 쓰고 있어서** 화면이 4px 를 쓰는 것을 100px 로 셌다.
"2020년대 140% 넘침" 이라는 **없는 문제**를 FAIL 로 띄웠다.
`hw`·`hh` 를 부르게 고치니 28% 였다. 이름표 수도 258 → 13 으로 바로잡혔다.
**화면·물리·검사가 같은 함수를 부르지 않으면 셋이 다 다른 말을 한다.**

그리고 `gl`(커서 빛)과 `ring`(초점 거리)은 다른 것이다.
`gl` 로 이웃을 판단했더니 결과를 눌러도 이어진 26개가 흐린 점 그대로였다.

### 화면 밖은 안 그린다

`draw` 시작에서 보이는 세상 범위를 한 번 구하고, 밖이면 건너뛴다.
선은 양 끝을 감싸는 상자로 판정한다. 3배로 확대하면 633개를 건너뛰어
그리기가 0.97 → 0.45ms 가 된다. `drawSkip`·`drawDrew` 로 몇 개를 건너뛰었는지 밝힌다.

### 무엇을 또렷하게 보일지 — 판단이 아닌 것으로만 고른다

결과 19개만 또렷하면 화면이 허전하고, **무엇이 있는지 몰라서 누를 수가 없다.**
그렇다고 전부 또렷하게 하면 이름표가 서로를 지운다.

그래서 일부만 고르는데, **무엇을 고르느냐가 곧 편집이다.**
"중요한 것" 을 고르는 순간 우리가 지면을 편집한 것이 된다. 기준은 이것만 쓴다.

| 쓰는 것 | 왜 |
|---|---|
| 이어진 선의 수 | **사실이다.** 우리가 정한 값이 아니라 데이터가 만든 값이다 |
| 종류별 몫 | 어느 종류가 중요한지가 아니라, **한 종류가 화면을 다 먹지 않게** 하는 것이다 |
| 같은 값이면 id 순 | 실행마다 달라지면 그 흔들림도 우리가 고른 것이 된다 |

**'화면 가운데에 가까운 것' 은 안 쓴다.** 카메라를 움직일 때마다 뽑히는 것이 바뀌어
이름표가 깜빡이고, '지금 보고 있는 곳' 이라는 우연이 기준이 되어 버린다.

**진영(side)은 기준에 안 넣는다.** 넣는 순간 균형이 아니라 연출이다.
대신 검사 46 이 매번 분포를 띄운다. 실측 (K=100):

| | gov | blue | red |
|---|---:|---:|---:|
| 전체 458개 | 74% | 12% | 7% |
| 뽑힌 100개 | 58% | 20% | 13% |

작게 뽑을수록 손으로 넣은 것(연결이 많다)이 더 뽑혀 blue·red 가 올라간다.
**손으로 맞추지 않고 이 사실을 밝힌다.**

### K 는 눈대중으로 못 올린다 — 고리가 먼저 넘친다

또렷한 노드는 이름표 상자만큼 자리를 차지한다. K 를 올리면 그 상자가 고리 넓이를 넘고,
**그때부터는 이름표를 계획해도 서로 지워서 안 그려진다 — 더 넣어도 화면에는 안 나온다.**

| K | 최대 고리 밀도 | 넘치는 연대 | 이름표(1440) |
|---:|---:|---:|---:|
| 70 | 69% | 0/9 | 47 |
| **100** | **69%** | **0/9** | **58** |
| 140 | 106% | 1/9 | 69 |
| 220 | 120% | 4/9 | 81 |

검사 22 가 넘침을 FAIL 로 잡는다. 그래서 이 값은 사람이 올릴 수 없다 — **검사가 정한다.**

### 공개된 정보를 모으는 것과 흩어진 채로 두는 것은 다르다

**우리가 모으면 그건 우리가 만든 명단이다.**

법제처 판례 상세의 `판례내용` 에는 `담당변호사 ○○○` 이 그대로 있다.
법원이 공개한 자료다. 그런데 **검색으로 흩어져 있던 이름이 한 곳에 모이면
그 순간 그것은 명단이 된다.** 공개 여부가 아니라 **집적** 이 문제다.

그리고 이 사이트는 JSON 을 통째로 내보낸다 —
**화면에 안 그려도 파일에 있으면 우리가 배포한 것이다.**
그래서 '화면에 안 그린다' 로는 못 막는다. **아예 안 담는 것**으로 막는다.

규칙 8 전문은 [docs/규칙.md](docs/규칙.md) 에 있다. 강제는 두 군데다 —
창고 검사 I(전문 칸·상세 호출 금지) · 지도 검사 47(이름 패턴).

**그리고 이름 패턴 검사는 처음에 29곳을 잡았는데 전부 오탐이었다** —
「특별검사」(법 이름) · 「유전자검사」 · 「증인에게」 · 「금지하는 검사」.
띄어쓰기와 성씨 목록을 함께 요구하니 0곳이 됐다.
**거짓 경보는 거짓 통과만큼 나쁘다.** 못 잡는 것은 규칙 8 에 표로 적어 두었다.

### 만 개를 그릴 때 비싼 것 — 실측 순서가 짐작과 달랐다

노드 11,094 · 선 13,635 에서 한 프레임이 44.7ms(22fps) 였다. 넷을 고쳐 26.5ms(38fps).

| 무엇 | 전 | 후 | 어떻게 |
|---|---:|---:|---|
| 힘 배열 | — | — | id 를 열쇠로 한 **객체 → Float64Array**. 매 틱 문자열 열쇠 22,188개를 만들고 있었다 |
| 목표 자리 | — | — | 반지름·각도는 **데이터가 정하므로 안 바뀐다.** 매 틱 ringR·cos·sin 을 다시 돌 이유가 없다 |
| 흐린 선 | 11.4ms | 7.5ms | 13,434개를 **한 경로에 담아 한 번 stroke**. 선마다 strokeStyle 을 만들고 있었다 |
| 노드 장식 | 23.4ms | 15.6ms | 이름표 없는 노드는 **원 하나만**. 후광·테두리는 가까이서 하나를 볼 때 뜻이 있다 |

**짐작한 순서가 틀렸다.** '노드가 많아서' 라고 생각했는데 노드만 그리면 3.9ms 였고
선이 7.5ms 였다. 그리고 정작 제일 큰 것은 **문자열 열쇠**였다 — 알고리즘이 아니라 자료구조다.

**묶어 그려도 래스터는 안 줄어든다.** 13,635개를 한 경로에 담으면 상태 변경은 사라지지만
픽셀은 그대로 칠한다 (11.4 → 7.5ms, 0 이 아니다). 더 줄이려면 **선을 덜 그려야 한다.**

### 고리 폭을 노드 수에만 비례시키면 옛 시대가 사라진다

사건 1만 개가 들어오자 2020년대가 폭의 절반을 먹고 **1940년대는 노드 4개에 폭이 거의 0** 이 됐다.
그 얇은 고리에 이름표 하나만 들어와도 **211%** 다.

균등하게 나누면 원래 문제(최근이 빽빽함)로 돌아간다. 그래서 **섞는다** —
`MIX=0.35` 로 '수' 와 '연도' 를 저울질한다. **순서는 안 바뀌고 간격만 완만해진다.**
그래서 화면에 "고리 간격 ≠ 시간 간격" 이라고 계속 적어 둔다.

### 판시사항에도 사람 이름이 있다 — 넘겨짚지 마라

"판시사항엔 이름이 없다" 고 생각했다. 검사 47 이 진짜를 잡았다.

```
[당사자]청구인 박○현 국선대리인 변호사 송한사(사임)
```

그리고 **익명 마스크(○)가 있으면 그 자리가 사람 이름이다.** `청구인 김○현` 처럼
성 한 글자만 남겨도, 사건번호와 함께 있으면 누구인지 좁혀진다.

**수술하듯 오려내지 않고 그 판시사항을 통째로 버린다.** 386개를 버렸다.
오려내다 놓치면 이름이 지도에 올라간다 — **남기는 쪽이 아니라 버리는 쪽으로 기운다.**

그리고 이름 패턴은 **띄어쓰기를 반드시 요구해야 한다.** `\s*` 로 뒀더니
죄명 「증인도피」 를 '증인'+'도피' 로 읽었다. 실제 이름은 늘 띄어져 있다.

### 지도에 그리는 것과 창고에 있는 것은 다르다

노드 11,094개를 **다 그렸더니 지도가 아니라 무늬가 됐다.**
바깥에 회색 점 고리가 둘러싸고 아무것도 안 읽혔다.

원인을 짐작하면 "선이 없는 점이 떠 있다" 지만 **선이 0개인 노드는 0개였다.**
진짜 값은 이것이다 — **연결이 1개뿐인 잎사귀가 8,959개(81%)**.
너무 많아서 안 읽히는 것이지 안 이어져서가 아니다.

그래서 **지도에 올리는 수를 정한다 (MAP_N=100).**
지우는 게 아니라 **옮기는 것이다** — 카드 목록과 '모두 보기' 에는 전부 있다.

고르는 기준은 판단이 아닌 것만 쓴다:
- 결과는 자리를 안 센다 (지도의 입구라 언제나 전부)
- 남은 자리는 **손으로 넣은 것 먼저, 그다음 자동.** 둘 다 이어진 선의 수 순
- 손을 먼저 두는 것은 **근거의 세기** 때문이지 '더 중요해서' 가 아니다

**100 은 스크린샷으로 골랐다. 계산이 아니다.**

| MAP_N | 눈으로 본 것 |
|---:|---|
| 30 | 결과는 또렷한데 둘레가 허전하다 — "연결고리" 라는 느낌이 약하다 |
| 50 | 읽히지만 아직 성기다 |
| **100** | **결과 19개가 다 들어오고 법·인물·사건이 이름과 함께 읽힌다** |
| 200 | 오른쪽에 이름 없는 점 무리가 생겨 다시 '무늬' 로 간다 |

### 지도에 올리는 것과 선을 그리는 것이 갈리면 그 화면이 다시 난다

초점을 켤 때 처음엔 이웃을 **연결 수 순으로 100개** 올렸다.
그런데 그리는 선은 6개(MAX_EDGES)다 — **선 없는 점 94개가 떠 있었다.**
고치기 전 화면과 똑같은 꼴이다.

**올리는 집합은 그리는 선에서 나와야 한다.** `setFocus` 가 고른 `kept` 로 `focusNb` 를 만들고
`planMap` 이 그것을 쓴다. 잘린 개수는 캔버스 배지('+N 모두 보기')와 카드가 계속 밝힌다.

### 고리 크기는 '창고에 몇 개' 가 아니라 '지도에 몇 개' 로 정한다

ROUT 를 11,094개 기준으로 2100 까지 키웠는데, 지도에 119개만 올리자
노드가 4,082px 에 흩어져 **결과가 화면 밖으로 잘렸다** (필요 배율 0.275, 바닥 0.45).
1150 으로 되돌리니 19개가 다 들어온다. **재는 대상이 바뀌면 상수도 다시 재야 한다.**

### 상수를 네 번 손으로 고쳤으면 그건 상수가 아니라 식이다

`ROUT`(고리 바깥 반지름)를 830 → 1050 → 1500 → 2100 → 1150 으로 고쳤다.
매번 이유가 같았다 — **재는 대상이 바뀌었는데 상수는 그대로였다.**
창고에 1만 개를 넣으면 키워야 하고, 지도에 48개만 그리면 줄여야 한다.

그래서 상수를 지우고 식을 뒀다 — **지도에 올린 노드가 차지하는 넓이**에서 구한다.

```
고리 넓이 = π·0.93·(ROUT² − RIN²)  ,  목표 밀도 45%
```

빽빽하면 이름표가 서로 지우고(검사 22), 성기면 "한쪽에 뭉쳤다" 가 된다(검사 26).
**두 검사 사이의 값을 사람이 매번 찾는 대신, 식이 찾게 했다.**

### 누르는 것만으로는 아무것도 안 움직인다 — 세 곳에서 새어 나왔다

지도에 30개만 그리기로 하자 초점을 켤 때 이웃이 새로 올라온다.
그때 노드가 한 프레임에 **170px** 씩 튀었다(검사 29). 원인이 셋이었고 **셋 다 짐작이 틀렸다.**

| 짐작 | 실제 |
|---|---|
| 고리 크기(ROUT)가 바뀌어서 | 잠갔는데 그대로 튀었다 |
| 각도를 다시 뿌려서 | 보존했는데 그대로 튀었다 |
| 고리표(연도→반지름)가 다시 만들어져서 | 잠갔는데 그대로 튀었다 |

**진짜 원인은 새로 올라온 노드가 지도 밖 좌표에서 날아온 것**이었다.
브라우저에서 노드 하나를 찍어 값을 비교하고서야 알았다 —
셋 다 0 이었고 움직인 것은 '새로 온 노드' 뿐이었다.
**자리는 데이터가 정하므로 날아올 이유가 없다.** 올라오는 순간 제자리에 놓는다.
셋을 잠근 것도 그대로 뒀다 — 잠글 이유가 각각 따로 있다.

### 크기는 순위를 보여주는 것이지 화면을 먹으라는 게 아니다

결과 노드 반지름이 `24 + √이웃수 × 4.8` 이었다. 자동 법률이 붙기 전에는 39px 였는데
이웃이 86개가 되며 **69px** 이 됐고, 결과 19개 중 **7쌍이 서로 겹쳐** 이름표가 가려졌다.
자란 것은 데이터지 뜻이 아니다. **자라는 몫에만 상한**을 뒀다 — 순서는 남고 화면은 안 먹는다.
29~42px, 겹침 1쌍.

### 카드는 우리 사정부터 읽히면 안 된다

공직선거법 카드가 이렇게 시작하고 있었다:

```
박근혜 2건 문재인 4건 윤석열 6건 이재명 4건
"그 정부가 밀어붙였다는 뜻이 아닙니다"
"자동으로 이은 것입니다…"
법제처 원문…
       ↓ 그러고 나서야
선거를 어떻게 치르는지 정한 법입니다
```

사람들은 **"공직선거법이 뭐야?"** 를 알고 싶은데 **우리 사정을 먼저 읽고 있었다.**

읽는 순서를 뒤집었다.

| 순서 | 무엇 |
|---|---|
| 1 | **이게 무슨 법인지** (제목 바로 아래) |
| 2 | 뭐가 바뀌었는지 |
| 3 | 이어진 것 |
| 4 | (접어서) 우리 사정 — 자동인지·원문·못 받은 것·정부별 건수 |

**크게 보여주고 아래에서 부정하면 사람들은 크게 본 것만 기억한다.**
정부별 건수(`박근혜 2건 문재인 4건…`)가 그랬다 — 크게 띄우고 바로 아래에서
"밀어붙였다는 뜻이 아닙니다" 라고 부정했다. 접어 넣었다.
**접는 것과 지우는 것은 다르다** — 한 번 누르면 전부 나온다.

그리고 같은 말이 두 번 나오고 있었다. `tip` 과 `body` 가 같은 문장으로 시작해서다.
앞부분이 겹치면 떼고 나머지만 쓴다(`dropLead`). 통째로 겹치면 안 쓴다.

### 첫 화면은 '가라앉은 뒤 맞추기' 만으로는 부족했다

들어오면 **확대된 채 떴다가 동그랗게 줄어들었다.** 원인이 둘이었다.

1. `cam` 이 배율 1(확대)에서 시작해 0.11씩 목표로 다가간다 — 그 과정이 그대로 보인다
2. 시작하자마자 `fit()` 을 불러도 **아직 물리가 안 돈 좌표**를 재서 배율 1.535 가 나왔다

배치는 결정론적이다(반지름=연도, 각도=흩뿌린 값). 그래서 **그리기 전에 미리 돌린다** —
실측 184틱 167ms 면 가라앉는다. 그 뒤 첫 배율을 애니메이션 없이 붙인다.

**강제:** 검사 48. 페이지가 `__camTrace` 에 배율을 적고 검사가 그걸 읽는다.
흔들림이 0.05 를 넘으면 FAIL. **계산으로 다시 만들지 않는다 — 화면이 실제로 쓴 값이다.**

그리고 검사 21·26 이 "0틱 만에 가라앉았다, 물리가 안 도는 것일 수 있다" 며 FAIL 했다.
**물리가 안 돈 게 아니라 먼저 돈 것이다.** 사전 틱을 합쳐서 보게 고쳤다.

### 어두운 배경에서 "옅게 칠하기" 는 그냥 검게 칠하는 것이다

자동으로 들어온 도형의 속을 26% 로 칠했다. 계산으로는 "배경보다 밝다" 가 맞다.
**실제 픽셀을 읽어 보니 (46,49,54) 였다** — 배경(14,15,18)보다 32 밝을 뿐이라
화면에서는 그냥 검게 보였고, 법 노드가 배경과 구별되지 않았다.

그리고 **알파가 두 번 곱해지고 있었다.** `globalAlpha` 가 이미 `n.a` 인데
거기에 0.26 을 또 곱해서, 흐린 노드는 (33,35,40)까지 떨어졌다.

| | 픽셀 |
|---|---|
| 배경 | (14,15,18) |
| 전 · 0.26 · n.a=1 | (46,49,54) |
| 전 · 0.26 · n.a=0.6 | (33,35,40) |
| **후 · 미리 섞은 색 0.55** | **(82,87,95)** |

고친 방법은 **알파를 겹치지 않는 것**이다 — 배경색과 미리 섞은 불투명 색을 만들어 칠한다.
그러면 노드가 흐려져도 '배경보다 밝다' 는 관계가 유지된다.

**값은 계산으로 고르지 않았다.** 캔버스에 실제로 그려 `getImageData` 로 픽셀을 읽어 골랐다.
(지도 캔버스는 외부 사진 때문에 오염돼 읽을 수 없다 — 같은 그리기 순서를
깨끗한 캔버스에 재현해서 쟀다.)

### 자동으로 도는 것에 건 제한이 사람 손도 막는다

노드를 끌면 어느 지점에서 벽에 막혔다. 범인은 tick 끝의 한 줄이었다.

```js
var BX=ROUT+340,BY=ROUT*0.86+300;
A.forEach(function(n){ n.x=Math.max(-BX,Math.min(BX,n.x)); … });   /* 예외가 없다 */
```

**자동으로 자리를 잡는 노드가 고리 밖으로 튀는 것을 막으려고 둔 벽인데,
사람이 끄는 것에도 그대로 걸렸다.** 실측이 그대로 말해 준다 —
손가락은 월드 2,687px 을 갔는데 노드는 1,374px 에서 멈췄고,
멈춘 자리가 `(-1179, -1021)` 로 **BX·BY 와 정확히 같았다.**

- 자동을 지키는 제한에는 **사람이 한 것**의 예외를 둔다 (`drag` · `__pulling` · `fixed`)
- 그렇다고 무제한으로 열지 않는다. NaN 이 한 번 새면 되찾을 방법이 없다 — 아주 넓게만 둔다
- **놓은 자리가 화면 밖이면 되돌려 놓지 않고 보여준다.** 되돌리는 것은 사람이 한 일을
  무르는 것이다. 넘친 만큼만 카메라를 밀어 지도 전체 인상이 안 바뀌게 한다

### 근거는 예외 없이 — 다만 '없다' 와 '못 찾았다' 는 다르다

근거 링크가 없는 노드를 종류별로 세니 **135개**였다. 하나씩 찾아 **20개**로 줄였다.

| 종류 | 처음 | 지금 | 무엇으로 |
|---|---:|---:|---|
| 인물 | 0/9 | **9/9** | 중앙선관위 선거통계시스템 (당선 기록) |
| 정당 | 0/13 | **13/13** | 중앙선관위 정당 정보 |
| 기관 | 0/7 | **6/7** | 각 단체 공식 홈페이지 |
| 사건 | 0/30 | **20/30** | 국가기록원 「기록으로 만나는 대한민국」 개별 주제 페이지 |
| 인물 연표 | 0/66 | **66/66** | **그 사람의 근거를 물려받는다** |

**연표는 따로 찾을 것이 아니었다.** 「음주운전 · 벌금 150만원」은 독립된 사건이 아니라
인물에 딸린 줄이다. 그 사람의 근거가 곧 그 줄의 근거다 —
데이터를 66번 복사하지 않고 화면이 물려받게 했다. 물려받은 것이라고 화면에 밝힌다.

**그리고 2,288개를 전부 열어 봤다. 안 열리는 것 0개.**
매 검사마다 열면 40분이라 아무도 안 돌리게 된다 — `tools/link-check.mjs` 가 전부 열어
`db/link_check.json` 에 남기고 검사가 그것을 읽는다. **오래된 결과는 결과가 아니라서**
검사가 잰 날짜를 함께 보고, 그 파일이 지금 지도보다 적으면 FAIL 한다.

남은 20개는 **찾아봤지만 없는 것**이다 — 국군방첩사령부령은 법제처 검색 API 로도 0건,
기자간담회 발언에는 원문이 없고, 이태원 참사를 다룬 국가기록원 주제 페이지는 아직 없다.
그때 국방부 메인이나 보도자료 목록을 넣으면 **'출처 있는 척'** 이 된다.
그래서 검사는 **비운 것이 아니라 이유 없이 비운 것**을 FAIL 로 잡는다.

### 글자로 적은 출처는 출처가 아니다

「1인 가구 35.5%」를 눌러도 근거 자료 자리에 「확인 중입니다」가 떴다.
**그 노드에는 `url`(KOSIS 통계표)이 있었다.** 카드가 그걸 안 읽고
`official2` 만 보고 있었다 — 있는 근거를 안 보여준 것이다.

세어 보니 244개에 열리는 링크가 없었다. 종류별로 갈라 보니 원인이 달랐다:

| 종류 | 처음 | 지금 | 무엇이었나 |
|---|---:|---:|---|
| 판례·헌재 | 2,124 | 2,141 | 카드가 `url` 을 안 읽었을 뿐 |
| 법 | 0/108 | **103/108** | 법제처 `/법령/{이름}` 으로 만들 수 있었다 |
| 결과 | 8/27 | **28/33** | 하나씩 찾아 열어 봤다 |
| 사건·인물·정당·기관 | 0/125 | 0/125 | 공식 페이지를 하나씩 찾아야 한다. 아직 못 했다 |

**200 이 곧 살아있음은 아니다.** 법제처는 없는 법 이름에도 200 과 함께
「국가법령정보센터 | 오류페이지」를 준다. status 만 보는 검사는 죽은 링크를 통째로 놓친다 —
그래서 검사 60 이 표본을 열어 **제목까지** 본다. 주입으로 확인했다.

**그리고 못 찾는 근거는 실제로 있다.** 국군방첩사령부령은 법제처 검색 API 로도 0건이고,
기자간담회 발언에는 원문 페이지가 없다. 그때 그럴듯한 링크(기관 메인·보도자료 목록)를
넣으면 **'출처 있는 척하는 사이트'** 가 된다. 그래서 기준을 이렇게 뒀다:

- 쓰는 것: **그 값을 찾을 수 있는 페이지** — 통계표·지표 페이지·법령 원문
- 안 쓰는 것: 기관 메인·보도자료 목록
- 못 찾으면 **비우고 왜 못 찾았는지 화면에 적는다**

검사도 그 약속으로 잰다 — **비운 것이 아니라 이유 없이 비운 것**을 FAIL 로 잡는다.
r3·q5 의 핵심어를 비우고 이유를 적은 것과 같은 처리다.

### 결과는 입구다 — 눌렀을 때 숫자만 있으면 들어올 이유가 없다

「1인 가구 35.5%」를 눌러도 **이게 무슨 숫자인지가 없었다.** 큰 숫자와
「2023년 35.5% · 2015년에는 27.2%였습니다 …표에서 그대로 옮겼습니다」 뿐이었다.
뒤의 문장은 **우리 사정**이다 — 어디서 옮겼는지는 사람이 궁금한 것이 아니다.

법 카드에 '무슨 법인지'(제1조)를 맨 위에 둔 것과 같은 자리를 결과에도 만든다.

| 순서 | 무엇 | 어디 |
|---|---|---|
| 1 | **이게 무슨 뜻인가** (노란 글씨) | `w` |
| 2 | 숫자 · 무엇을 센 것인가 | `big` · `m` |
| 3 | 예전엔 이랬는데 지금은 이렇다 | `c` · 시계열 한 줄 |
| 4 | 그다음이 우리 사정 | 출처 · 근거 |

- **숫자는 한 곳에만 둔다.** `db/result_easy.json` 에는 말만 있고 값은 노드(`big`·`series`)에서
  온다. 두 곳에 적으면 갈라지고, 갈라지면 어느 쪽이 맞는지 알 수 없다
- 한자어를 그대로 옮기지 않는다 — 「상대적 빈곤율」은
  **「남들보다 훨씬 적게 버는 사람 몫」** 이다. 검사 58 이 낱말 그물로 잡는다
- **같은 자리를 둘이 맡으면 같은 말이 두 번 나온다.** `w` 를 넣자마자 그 아래
  `tip` 이 거의 같은 문장을 또 썼다 — `w` 가 있으면 `tip` 을 안 그린다

### 검색이 적게 나오는 것과 지도에 적게 올린 것은 다른 문제다

「살인」이 3건만 나왔다. 세 숫자를 나란히 놓으니 어디서 빠지는지 바로 보였다.

| 낱말 | 창고 사건명 | 지도 노드 | 검색 |
|---|---:|---:|---:|
| 살인 | 504 | 3 | 3 |
| 사기 | 2,849 | 65 | 65 |
| 절도 | 1,067 | 3 | 3 |

**검색은 지도에 있는 것을 다 찾고 있었다.** 빠지는 곳은 창고 → 지도다.
판례는 법을 통해서만 올라오는데(결과 → 법 → 판례) 살인 판례가 붙을 「형법」이 지도에 없다.

억지로 올릴 수는 있다 — r11(검찰청 폐지)의 `keys` 에 `형법` 을 넣으면 된다.
**그건 글자가 같은 것과 주제가 같은 것을 섞는 일이다.** 안 한다.

그래서 지킬 수 있는 약속만 검사한다(59번):
- **지도에 있는 것이 검색에서 빠지면 FAIL** — 이건 우리가 고칠 수 있다
- **격차를 화면에 밝히는가** — 말없이 두면 '우리 지도에 3건' 이 '세상에 3건' 으로 읽힌다.
  숫자는 지어내지 않는다(정적 사이트라 창고 건수를 모른다). 법제처 원문 검색으로 보낸다

**한 숫자만 보면 원인을 못 찾는다.** 셋을 나란히 놓아야 어디가 병목인지 보인다.

### 어려운 글의 절반은 우리가 쓴 것이 아니라 **옮겨 담은 것**이었다

검사 30 이 오래 이렇게 말하고 있었다 — 60자 넘는 문장 536개 · 어려운 말 837개.
"쉬운 말로 옮기는 일이 산더미" 로 보였다. 그런데 어디서 왔는지 세어 보니 이랬다:

| 어디 | 60자 넘음 | 어려운 말 |
|---|---:|---:|
| 판례 | 433 | 115 |
| 헌재 | 94 | 721 |
| 법 | 9 | 1 |

판례·헌재의 본문은 우리가 이렇게 쓰고 있었다 —
`법원 판례입니다. 사건 이름은 "…" 이고 사건번호는 …입니다.`
**사건 이름을 통째로 인용한 것**이다. 그 이름은 재판부가 쓴 원문이라 길고 어렵다.
카드에는 제목으로 이미 나와 있으니 **같은 말이 두 번** 나오기도 했다.

한 줄을 지우니 536 → 9, 837 → 2 가 됐다. 남은 9개는 법의 제1조(목적) 원문이었고
그건 쉬운 말로 옮겼다(`db/law_easy.json`). 최종 **0 / 0**.

- 어려움을 세기 전에 **그 글자가 어디서 왔는지** 먼저 본다.
  우리가 쓴 것인지, 원문을 옮겨 담은 것인지
- 원문은 카드가 이미 보여준다. **본문은 우리가 하는 말만 한다**
- 세 조건(60자 아래 · 어려운 말 옆에 괄호 · 자연스러움)이 부딪히면
  **문장을 나눈다.** 설명만 뒤로 빼면 어려운 말 옆에 괄호가 없어져 도로 걸린다

### 자동을 지키는 제한은 **두 번** 사람 손을 막았다

같은 실수가 한 주에 두 번 났다. 둘 다 "자동으로 도는 것이 튀지 않게" 두었던 제한인데
사람이 직접 하는 것에도 그대로 걸렸다.

| 무엇 | 어디 | 실측 |
|---|---|---|
| 노드를 끌 때 벽 | `tick()` 끝의 `BX/BY` 클램프 | 손가락 2,687px · 노드 1,374px |
| 화면을 끌 때 세로 | `clampY()` (fade 안에서 매 프레임) | 아래 5% · 위 26% · 좌우 100% |

두 번째가 더 찾기 어려웠다. **가로는 멀쩡하고 세로만 막혀서** 팬 코드에는 아무 문제가
없어 보였기 때문이다. 팬은 `cam.x`·`cam.y` 를 그냥 더할 뿐이고, 되돌리는 것은
**다른 함수가 다음 프레임에** 하고 있었다.

- 제한을 둘 때 **누가 그 값을 움직이나**를 함께 적는다. 자동만인가, 사람도인가
- 사람이 손댄 뒤에는 규칙을 바꾼다 (`camUser` 로 가른다) — 첫 화면 규칙은 첫 화면까지다
- 대신 **완전히 놓아주지는 않는다.** 지도를 화면에서 잃으면 되찾을 방법이 없다.
  네 방향 모두 최소한만 겹치게 두고, 그 최소값도 검사가 지킨다
- **한 방향만 막히면 그 방향을 재는 검사가 없었다는 뜻이다.** 네 방향을 다 잰다

### 자리를 정하는 곳이 둘이면 매 프레임 서로 지운다

끌면 이어진 것이 **뚝뚝 끊기며** 따라왔다. 원인이 셋이었고 셋 다 같은 종류다 —
`pullNeighbors` 가 고무줄로 당겨 놓은 것을 **같은 프레임 안에서 다른 코드가 지웠다.**

| 무엇이 지웠나 | 그래서 |
|---|---|
| 각도 이징 루프의 `n.x=cos(a)*R` | **반지름이 매 프레임 원위치로 스냅**돼 각도 성분만 남았다 |
| 속도 적분(목표 자리로 당기는 용수철) | 고무줄과 반대로 당겨 뒤로 채였다 |
| 겹침 해소 8회 | 따라오던 것끼리 서로 밀어냈다 |

실측으로 갈랐다. 고치기 전 **이웃이 한 프레임에 55.3px** 뛰었는데
끈 노드는 17.3px 이었다. **따라오는 것이 끄는 것보다 빨랐다** — 그건 따라오는 게 아니다.

셋을 다 빼고 나니 이웃 최대 17.1px · 끈 노드 17.8px · 안 움직인 프레임 0/2240 이 됐다.

- 한 값을 두 곳에서 정하면 **눈에는 떨림으로 보인다.** 로그에는 아무것도 안 남는다
- 판정 기준을 절대 px 로 두지 마라. **끈 노드의 걸음**과 견주면 손가락 속도와 무관하게 옳다
- 예외를 한 군데 넣었으면 **같은 종류의 다른 루프에도 넣었는지 본다.**
  뒤쪽 반지름 복원 루프에는 이미 `__pulling` 예외가 있었고 앞쪽에만 빠져 있었다

### 이름이 겹치면 도구가 엉뚱한 곳을 고친다 — 범위를 좁혀서 찾아라

분야 `edu`(교육)를 만들었더니 `pick-index.mjs` 가 CATMAP 이 아니라
**인물 노드의 학력**을 고쳤다.

```js
edu:[['초등학교','삼계국민학교 졸업 (1976)','hs_edu']]   /* ← 학력에 통계 id 가 들어갔다 */
```

`\n edu:\[` 로 **파일 전체**에서 찾았기 때문이다. 인물 노드(85,051번째 글자)가
CATMAP(165,877번째)보다 앞에 있어서 그쪽이 먼저 걸렸다.

**문법 오류가 안 나고 검사도 안 잡는다.** 배열에 문자열 하나가 더 늘었을 뿐이다.
화면에서 그 사람의 초등학교 학력에 이상한 값이 붙어 있는 것을 눈으로 봐야 안다.

- 코드를 고치는 도구는 **고칠 블록을 먼저 잘라내고** 그 안에서만 찾는다
- 짧은 이름(`edu` `tax` `for`)은 다른 필드 이름과 겹친다고 보는 편이 안전하다
- 바꾼 뒤 **바꾼 자리를 다시 읽어 확인한다.** 「몇 건 바꿨다」 는 어디를 바꿨는지 말해주지 않는다

### 되돌리는 길이 둘이면 갈라진다 — 그리고 갈라진 쪽이 사람을 가둔다

'뒤로' 를 만들 때 초점을 켤 때마다 `history.pushState` 로 한 칸씩 쌓았다.
브라우저 뒤로가기는 잘 됐다. 그런데 **화면의 '뒤로' 로 되돌아오면** 우리 이력만 줄고
브라우저 칸은 그대로 남는다. 그 뒤에 브라우저 뒤로가기를 누르면 아무 일도 안 일어난다 —
몇 번을 눌러도. **나가지도 못하고 되돌아가지도 못한다.**

고친 방법은 **칸을 하나만 까는 것**이다. 그 칸을 소비하면, 되돌아갈 게 남아 있을 때만 다시 깐다.

- 되돌아갈 게 **남았으면** 다시 깐다 → 뒤로가기가 지도에서 한 칸 뒤로 간다
- **없으면** 안 깐다 → 그다음 뒤로가기로 **사이트를 나갈 수 있다**

늘 다시 까는 쪽이 "사이트를 안 나가게 한다" 는 요구에 더 충실해 보인다.
그건 되돌려 주는 것이 아니라 **가두는 것**이다. 나갈 수 없는 것은 고장과 구별되지 않는다.

같은 이유로 **뒤로 갈 게 없으면 버튼을 숨긴다.** 눌러도 아무 일 없는 버튼은
"안 되는 버튼" 이 아니라 "고장난 사이트" 로 읽힌다.

**강제:** 검사 54. 세 번 누르고 세 번 뒤로 눌러 처음 상태로 돌아오는지 보고,
이력이 비면 버튼이 숨는지 본다. `goBack()` 을 직접 부르지 않고 **버튼을 실제로 누른다** —
직접 부르면 버튼이 안 걸려 있어도 통과한다.

### 테두리는 노드 모양을 따라가야 한다

선택·커서 표시를 늘 원으로 그렸다. 법 노드는 가로로 누운 사각형인데
그 둘레에 원을 그리니 **원이 사각형을 감싸 덮어** 정작 모양이 안 보였다.
`shapePath(n,pad)` 하나를 그리기와 테두리가 함께 쓴다 — 둘이 갈라질 수 없다.
