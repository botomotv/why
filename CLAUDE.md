# 왜(Why)

한국 정치·정책의 인과관계를 지도로 보여주는 웹사이트.
"이 숫자가 왜 이렇게 됐나"를 결과에서 거꾸로 추적한다.
무료 공개. 광고·후원·구독 없음. **신뢰가 유일한 자산이다.**

## 구조

```
index.html          단일 HTML 파일. 데이터 + 렌더링 + 물리 레이아웃이 전부 여기.
                 아직 쪼개지 않는다. 렌더링 분리는 나중 단계다.
test/check.cjs   jsdom 점검 스크립트. 규칙 강제는 전부 여기에 있다.
docs/규칙.md      절대 규칙 7개 + 왜 그런 규칙이 있는지.
```

## 이 프로젝트에서 가장 중요한 원칙

**규칙은 프롬프트에 있으면 샌다. 반드시 자동 검사로 강제한다.**

이 파일에는 "왜 이 규칙이 있는지"만 쓴다. 강제는 전부 `test/` 밑에 둔다.
사람이 지키는 규칙은 언젠가 깨진다. `npm test`가 지키는 규칙만 남는다.

규칙 7개 전문과 그 이유는 [docs/규칙.md](docs/규칙.md)에 있다. 요약하면:

1. 확정된 공식 기록만 — 수사 중인 의혹은 절대 안 넣는다
2. 확인 안 된 숫자는 지어내지 말고 "확인 중"으로 비워둔다
3. **"재임 중" ≠ "밀어붙임"** — 대통령 관계는 공포일자 × 재임표 계산으로만 만든다
4. 시간 순서를 인과관계로 단정하지 않는다 — 결과 카드에 교란 요인을 함께 적는다
5. 법안마다 `limit`(남은 문제·비판) 칸을 채운다. 한쪽 진영에만 붙이지 않는다
6. 공공누리·퍼블릭도메인 사진만. `photoLicense` 없으면 실루엣으로 떨어진다
7. 모든 노드에 출처(`src`)를 표기한다

규칙 3이 가장 쉽게 깨진다. **발의자·소관부처 필드에서 대통령을 끌어오는 코드 경로를
아예 두지 않는다.** 그런 경로가 생기는 순간 "○○ 정부 때 통과된 법"이
"○○가 밀어붙인 법"으로 자동 변환된다.

## 선은 인과가 아니다

**이 사이트는 인과를 주장하지 않는다.** 관련 있는 것들을 이어주고 판단은 보는 사람이 한다.

관계선의 뜻은 **"같은 주제·같은 시기에 있었다"** 이지 "이것 때문에 저렇게 됐다" 가 아니다.
그래서 선은 사실 판정이 가능하고, 나중에 자동 생성도 가능해진다.

**사람이 이해할 수 있는 근거가 안 나오는 연결은 애초에 만들지 않는다.**

카드의 관계 항목마다 `왜 이어졌나 · 같은 분야(의료·저출생) · 시기 1년 차이` 가 붙는다.
이 한 줄을 만들 수 없으면 그 연결은 넣지 않는다. 근거를 못 대는 선은 주장이지 기록이 아니다.

- `lead`(밀어붙임) · `against`(반대·대립) · `term`(그때 정권) 은 **공식 기록이라 그대로 둔다.**
- `l[3]==='result'` 는 **'결과' 가 아니라 '근거(l[5])가 붙은 연결'** 이라는 뜻이다.
  값 이름은 데이터 63건이 쓰고 있어 그대로 두고, 창고로 옮길 때 바꾼다.
- 방향(누가 앞이고 누가 뒤인지)으로 뜻을 만들지 않는다.
  전에는 같은 선이 보는 방향에 따라 '배경·원인' 과 '결과' 로 갈렸다 —
  그건 데이터가 아니라 화면이 만들어낸 주장이었다.

**지도에 그리는 선은 노드당 6개까지.** 실측으로 정한 값이다 —
이웃 6개까지 이름표가 100% 읽히고, 8개에서 63%, 10개를 넘으면 선 간격이 3.2° 로 붙는다.
**잘린 개수는 항상 화면에 밝힌다.** 말없이 자르면 "이게 전부" 라는 거짓말이 된다.

자세한 설계는 [docs/자동연결.md](docs/자동연결.md).

## 검사를 검사한다

**"통과했다"는 말을 믿기 전에, 그 검사가 무엇을 봤는지 확인한다.**

깨진 검사는 없는 검사보다 나쁘다. 없는 검사는 불안하게 만들지만,
깨진 검사는 안심시킨다. 안심한 채로 틀린 걸 배포한다.

실제로 이 프로젝트에서 **일곱 번** 나왔다. 일곱 번 다 초록불이었다.

| # | 무엇이 | 어떻게 거짓말했나 | 지금 막는 것 |
|---|---|---|---|
| 1 | `process.exit(0)` 하드코딩 | FAIL이 몇 건이든 `npm test`가 통과했다. 강제가 아니라 장식이었다 | `exit(fails.length?1:0)` |
| 2 | "겹침 0" | 노드 **원**의 중심거리만 쟀다. 실제로 겹치는 건 **이름표 박스**였다 (290쌍) | 이름표 박스 겹침 검사 |
| 3 | `measureText` 스텁 | `글자수 × 6.2px`. 한글은 12px 폰트에서 폭이 거의 12px다. **이름표 폭을 절반으로 재고 있었다** | 전각/반각 폭 계산 |
| 4 | 죽은 CSS 규칙 | 잘린 셀렉터가 다음 규칙을 삼켜 `body.panelon body.panelon .pclose`가 됐다. `body` 안에 `body`는 없으니 **영원히 매칭 안 된다.** 카드 ✕ 버튼이 브라우저 기본 회색 네모로 나왔다 | 12번 검사 |
| 5 | 스크립트 에러 무시 | `jsdomError`를 **모으고만 있었지 판정에 안 썼다.** 스크립트가 중간에 죽어도 PASS였다 | 14번 검사 |
| 7 | 셀렉터 중복 검사 | 블록 머리에 **앞선 주석이 그대로 붙어** 와서 같은 `.rolebar` 가 서로 다른 문자열이 됐다. 중복을 하나도 못 잡았다 | 머리에서 주석 제거 |
| 6 | `innerText` 로 화면 글자 검사 | **jsdom 은 `innerText` 를 지원하지 않는다.** `undefined` 가 나오고 `|| ''` 가 그걸 삼켜서, 화면에 실명을 되돌려 넣어도 검사가 통과했다 | `textContent` 사용 |

**공통점: 일곱 번 다 문법 오류가 아니었다.** 코드는 멀쩡해 보였고 검사는 초록불이었고
화면만 틀렸다. 그래서 사람이 눈으로 보기 전까지 아무도 몰랐다.

**6번과 7번은 검사를 새로 만들면서 그 자리에서 났다.** 둘 다 실패를 주입해 보지 않았으면
거짓 보증을 그대로 커밋할 뻔했다. 7번은 "중복을 하나도 못 잡는 중복 검사" 였다 —
**아무것도 안 잡는 검사는 언제나 PASS 라서 가장 오래 산다.**
**새 검사를 넣을 때마다 실패를 주입해서 실제로 FAIL 이 나는지 본다** — 아래 1번이 그 이유다.
그리고 **jsdom 은 브라우저가 아니다.** `innerText` `offsetWidth` `getComputedStyle` 의
레이아웃 값처럼 지원이 없거나 다른 API 가 있다. 스텁·폴백이 값을 삼키면 검사는 조용히 통과한다.

**5번이 제일 무섭다.** 스크립트가 중간에 죽어도 **함수 선언은 호이스팅되어 이미 정의돼 있다.**
그래서 카드도 열리고 검색창도 보인다. 겉으로 멀쩡한데 그 뒤 초기화가 전부 안 돈다.
3번도 같은 종류다 — 2번을 고쳐도 겹침이 절반만 잡혔을 것이다.

**검사를 고쳤다는 사실이 그 검사가 맞다는 뜻은 아니다.**

## CSS 규칙 — 미디어쿼리는 항상 파일 맨 끝

**미디어쿼리는 `<style>` 맨 끝에 모은다. 그 뒤에 일반 규칙을 쓰지 않는다.**

같은 특정도면 **나중에 선언한 쪽이 이긴다.** 미디어쿼리 뒤에 일반 규칙을 두면
반응형이 조용히 무력화된다. 문법 오류가 없어서 아무도 모른다.
화면만 틀리고, 코드는 멀쩡해 보인다.

세 번 났다. **세 번 다 같은 원인인데 각각 다른 버그처럼 보였다.**

| 셀렉터 | 겉으로 보인 증상 |
|---|---|
| `.brand` | 폰에서 제목이 "왜왜"로 두 번 렌더 |
| `.search input` | 폰 검색 안내문이 사라짐 (입력창은 388px로 넓게 열린 채) |
| `.ub` | '처음으로' 버튼이 필터를 안 눌러도 항상 보임 |

앞의 둘은 `font-size:0` / `width:38px`으로 **숨기려던 것이 안 숨겨진** 경우다.
세 번째는 `[hidden]`을 이겨버린 경우다. 개별 버그로 고치면 네 번째가 또 온다.

**강제:** `test/check.cjs` 11번 검사. 미디어쿼리 뒤에 일반 규칙이 하나라도 있으면 **FAIL**.
미디어쿼리 안에 같은 셀렉터가 있으면 "무엇을 덮어쓰는지"까지 지목한다.

### 그래서 생긴 두 번째 규칙

미디어쿼리를 뒤로 몰면서 **"같은 특정도면 뒤엣것이 이긴다"가 이 파일의 기본 구조**가 됐다.
그 구조를 자꾸 잊는다.

**`display`를 끄려면 앞에 `none`을 넣지 말고, 뒤에 있는 원본 규칙을 고쳐라.**

```css
.rolebar{display:none}                    /* ✗ 앞에 넣어봐야 */
.rolebar{position:fixed;display:flex;…}   /*   뒤엣것이 이긴다 */
```

`.rolebar` 를 이렇게 껐다가 화면에 그대로 보였다. 브라우저에 규칙을 물어보고서야 알았다.
`display` 뿐 아니라 모든 속성이 같다 — **끄고 싶으면 켜는 곳을 고친다.**

**강제:** 12번 검사.
- 앞의 `display:none` 이 뒤의 `display:…` 에 지는 경우 → **FAIL**
- 그 밖에 같은 셀렉터를 뒤에서 덮어써 앞 선언이 죽은 경우 → WARN (현재 14건, 정리 대상)

### 그리고 세 번째 규칙 — 문서에 쓴 조건이 코드에도 반영됐는지 확인한다

**말과 값이 따로 놀면 문법 오류가 없다.** 그래서 아무도 모른다.

두 번 났는데 둘 다 같은 종류다.

| 문서에 쓴 말 | 코드가 한 일 |
|---|---|
| "표결은 20대부터만 제공된다" | 전 대수로 시간을 셌다 (27.8시간 vs 실제 17.8시간) |
| 표 머리 "1초 간격" | 실행 시 간격(GAP_MS)으로 계산 — 0으로 테스트하니 0.0시간 |

**같은 쪽에 조건을 써놓고 숫자는 그 조건을 안 지켰다.**
글을 읽는 사람은 숫자를 믿고, 숫자를 만든 코드는 글을 안 읽는다.

- 문서에 조건을 쓰면 **그 조건이 코드 어디에 있는지** 같이 적는다
- 조건 값은 **한 곳에만 둔다.** `VOTE_MIN_AGE` 는 `tools/lib/api.mjs` 하나뿐이고
  `count` 와 `collect` 가 그걸 import 한다. 각자 갖고 있으면 어긋난다
- 표 머리에 단위·조건을 썼으면 **그 단위로 계산한다**

### 값을 잴 때는 전환(transition)을 끄고 잰다

**브라우저 값도 그대로 믿으면 안 된다.** 이번이 세 번째다.

`.rail` 의 `transform` 이 `translateY(1492px)` 로 나와서 "901~1000 구간에서 패널이
화면 밖" 이라는 버그를 보고할 뻔했다. **멈춰 있던 전환의 잔상이었다.**

```js
var st=document.createElement('style');
st.textContent='*{transition:none !important;animation:none !important}';
document.head.appendChild(st);
el.getBoundingClientRect();          // 강제 리플로
var v=getComputedStyle(el).transform;  // 이제 진짜 값
st.remove();
```

- **`getComputedStyle` · `getBoundingClientRect` 를 읽기 전에 전환을 끈다**
- 브라우저 창이 뒤로 가면 전환과 `requestAnimationFrame` 이 멈춘다.
  그 상태에서 잰 값은 **중간값**이지 최종값이 아니다
- 재기 전에 탭을 앞으로 가져오거나, 위처럼 전환을 끄고 리플로를 강제한다

**jsdom 은 값을 안 주고, 브라우저는 틀린 값을 준다.** 종류가 다른 거짓말이다.

### 그래서 지킬 것

1. **검사에 실패를 주입해서 실제로 FAIL이 나는지 본다.**
   노드 하나의 `src`를 일부러 지운 사본으로 돌려 `exit 1`을 확인하는 식이다.
   통과하는 것만 보고 검사를 믿지 않는다.
2. **검사 이름에 무엇을 재는지 박는다.** "겹침"이 아니라 "점겹침"·"이름표겹침"이다.
   이름이 모호하면 사람이 알아서 넓게 해석한다.
3. **0은 의심한다.** 0건은 "문제 없음"과 "안 보고 있음"을 구별하지 않는다.
   `이름표 0/171개 그려짐`처럼 **분모를 같이 출력한다.**
4. **가짜 값(스텁·목)은 실물과 대조한다.** jsdom 숫자와 브라우저 숫자를 나란히 본다.
   지금은 이름표 겹침이 jsdom 291쌍 : 브라우저 268쌍으로 맞는다. 안 맞으면 스텁이 틀린 것이다.
5. **계산이 통과해도 눈으로 본다.**
   부채꼴 모으기를 만들 때 수치는 전부 정상(6/6 · 반지름 흔들림 0)이었는데
   **화면에서 원 세 개가 포개져 있었다.** 각도를 충돌 해소 뒤에 강제해
   충돌 분리를 덮어쓴 탓이다. 스크린샷을 안 봤으면 그대로 커밋했다. 계산으로 못 잡는 게 있다.
   결과 노드 전체의 중심으로 화면을 맞췄더니 고리 배치라 한가운데가 빈 곳이었다.
   닫기 버튼이 서랍 내용을 가렸다. 둘 다 수치는 정상이었고 화면만 틀렸다.

## 데이터 구조

노드 · 관계 · 역할 세 가지뿐. 나라가 바뀌어도 틀은 동일하다.

- **노드** `N` — `{id, t, side, lab, src, ...}`
  - `t`: `bill`(법안) / `result`(결과) / `person` 등
  - `side`: 진영. `blue` / `red` / `gov` / `gold`
  - `ghost`: 런타임 파생 노드. 선언 164개 + 파생 67개 = 실제 231개
- **관계** `L` — `[from, to, label, role, sentence, evidence]`
  - `role`: `lead`(밀어붙임) / `term`(그때 정권, 보라색) / 등
  - `l[5]`(evidence)가 있어야 인과 관계로 인정된다

## 작업 방식

```bash
npm test
```

- 파일을 고칠 때마다 `npm test`를 돌리고, **통과한 것만 커밋한다.**
- **통과했다는 출력을 그대로 믿지 않는다.** 위 "검사를 검사한다"를 볼 것.
- **정규식으로 코드 블록을 삭제하지 마라.** 문법 검사는 통과하는데 함수가 통째로
  날아갈 수 있다. 실제로 그런 사고가 한 번 났다. 반드시 jsdom으로 띄워서 확인한다.
- 해상도 **412(폰) · 840(폴드) · 1440 · 1920** 에서 모두 확인한다. `check.cjs`가 자동으로 돈다.
- 확인 안 된 사실은 넣지 말고, **뭘 확인 못 했는지 말한다.**

## 지금 알려진 상태

- **진영 분포가 기울어 있다** (blue 93 / red 55 / gov 35). 규칙 5와 부딪히는 지점.
  API로 발의·표결 데이터를 채우면 자동으로 완화된다. **손으로 고치지 않는다.**
- `limit` 칸이 빈 법안 4개: `b6` `b7` `b9` `b11`. 지금은 WARN.
- **결과 노드가 19개뿐이다. 이게 진짜 병목이다.**
  법안 수가 아니라 결과 숫자가 성장 단위다.
- `official2` 링크가 붙은 노드는 `r6` 하나뿐.
- API 수집기는 아직 없다. 인증키가 없어서다.

## 창고 (SQLite)

수집물은 `db/warehouse.db` 에 담고 **커밋하지 않는다.** 스키마와 규칙만 남긴다.

```
db/schema.sql       원본층 · 가공층 · 관계층
db/rules/*.sql      선을 만드는 규칙. 규칙 하나가 파일 하나다
db/seed.sql         규칙 목록 (rule.reads = 그 규칙이 읽어도 되는 표)
tools/count.mjs     대수별 건수 세기. 수집 전에 규모를 먼저 안다
tools/collect.mjs   원본층 수집기. 중단해도 이어서 받는다
test/warehouse.cjs  검사 A~E
```

**선은 규칙 없이 못 들어간다.** `link.rule` 이 `NOT NULL REFERENCES rule(name)` 이고
`link.why`(왜 이어졌나)도 `NOT NULL` 이다. 근거를 못 대는 선은 주장이지 기록이 아니다.

**규칙 3 은 `rule.reads` 로 강제한다.** `term_by_promulgation` 이 읽어도 되는 표는
`bill, president_term` 둘뿐이다. 검사 B 가 `.sql` 원문을 훑어 그 밖의 표가 나오면 FAIL.
검사 D 는 **발의자에서 대통령을 끌어오는 가짜 규칙을 주입해 B 가 실제로 FAIL 하는지 본다.**
D 를 빼지 않는다 — 아무것도 안 잡는 검사는 언제나 PASS 라서 가장 오래 산다.

**API 가 안 주는 시기가 두 군데 있다.** 실측이다 (`probe-out/_COUNT.md`).

| | 제공 | 못 얻는 시기 |
|---|---|---|
| 발의법률안 | 제10대~ | **제1~9대 (1948~1979) 전부 0건** |
| 개인별 표결 | 제20대~ | 제19대 이전 |

**0 은 "법안이 없었다" 가 아니라 "API 가 안 준다" 는 뜻이다.**
지도의 **1945~1980 근현대사 분야는 이 구멍 위에 있다** — 자동 수집이 채워주지 않는다.
법제처 근대법령을 확인해야 하는데 아직 못 했다.
수집기는 건너뛴 대수를 화면에 밝힌다. 말없이 건너뛰면
"표결이 없는 법안" 과 "표결을 안 받은 법안" 이 구별되지 않는다.

자세한 건 [docs/창고설계.md](docs/창고설계.md).

## 화면에 안 그리는 것으로는 못 막는다

이 사이트는 정적이다. **JSON 을 통째로 받아서 그린다.**
법안 카드에 찬성자 이름을 안 늘어놓아도, 내보낸 파일에 들어 있으면
주소창에 파일 이름만 치면 전부 나온다.

**그래서 이건 화면 결정이 아니라 내보내기 결정이다.**
개인 표결은 인물별 파일로 쪼갠다 — 전량을 받으려면 3,295번 받아야 한다.
못 막지만 늦춘다. **우리가 명단을 만들어 배포하는 쪽이 되지 않는 것**이 목적이다.

받되 내보내지 않는 필드가 있다: `BIRDY_DT` `NAAS_TEL_NO` `NAAS_EMAIL_ADDR`.
창고에는 원본이 남고 지도로는 안 나간다. 매퍼가 `member` 표에 넣지 않는다.

카드 설계와 위험 목록은 [docs/인물법안카드.md](docs/인물법안카드.md).

## 로드맵

한국판 완성 → 미국 · 일본 확장 → 다국어 지원.
